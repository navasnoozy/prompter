# Generated command permissions

The TOML files under `autogenerated/` are produced from
`src/command_manifest.rs` by `tauri-build`. They are intentionally checked in
so capability review and clean-checkout tooling see the exact native command
surface. Regenerate them with a Rust build after changing the manifest, then
review and commit the resulting permission diff.
